<%= simple_form_for(@cocktail) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>s
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
